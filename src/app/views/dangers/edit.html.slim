.page-header
  = link_to dangers_path, class: 'btn btn-secondary float-right' do
    = raw "#{icon("fas", "angle-left")} #{t('activerecord.models.danger', default: "Danger")} #{t('interface.index')}"
  h1 = "#{t('activerecord.models.danger', default: "Danger")} #{t('interface.edit')}"
= render 'form'
